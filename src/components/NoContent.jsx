const NoContent = ({ content }) => {
  return (
    <>
      <div className="no-content-wrapper">
        <span>There are no produce listings at the moment.</span>
      </div>
    </>
  );
};

export default NoContent;
